Full suite checkpoint is green: go build ./..., go vet ./..., go test ./..., and a go test -json skip scan found no test-level skips. Help output for top-level, create, and init shows author/critic defaults and phase descriptions with no --planner. The only stale reference was a regression-test literal in cmd/vibedrive/main_test.go; it now constructs the removed legacy flag without leaving stale user-facing grep matches.
