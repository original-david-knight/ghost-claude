Added internal/create StageRunner with injected launcher, confirm function, and output writer. Critic feedback is captured from critic stdout while streaming to terminal output, then passed directly in the follow-up author prompt; no critic artifact is persisted. Cancellation now propagates without rollback, so partial agent writes remain on disk and only successful author phases update create state.
